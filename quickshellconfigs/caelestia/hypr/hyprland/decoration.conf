decoration {
    rounding = 20
    blur {
        enabled = false
        xray = false
        special = false
        ignore_opacity = true  # Allows opacity blurring
        new_optimizations = true
        popups = true
        input_methods = true
    }

    shadow {
        enabled = false
    }
}
